import { SxProps } from '@mui/material';
import { Theme } from '@mui/system';

export const tableWidgetStyles: SxProps<Theme> = {
  maxHeight: 'calc(100vh - 84px)',
  minHeight: 'calc(100vh - 84px)',
  height: 'auto',
  overflow: 'hidden',
  bgcolor: 'white',
  borderRadius: '12px',
  border: '1px solid #E0E5EB',
};
